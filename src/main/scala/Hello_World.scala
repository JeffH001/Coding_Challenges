import scala.io.StdIn.readLine

object Hello_World {
	def main(args: Array[String]): Unit = {
		println("Hello, world!")

		// Test code from interview:

		// val text = readLine()
		// println(s"Length: ${text.length}")

		// val someArr = Array(1, 2, 3, 4, 5, 6, 7)
		// println(s"Min: ${someArr.min}\nMax: ${someArr.max}\nAverage: ${someArr.sum / someArr.length}")
	}
}